{
  "name": "chronolog",
  "version": "0.1.0",
  "private": true,
  "description": "A retrospective time logging application.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @chronolog/web",
    "dev:web": "npm run dev --workspace @chronolog/web"
  }
}
